# Object collection — third iteration

Branch: `explore/color-studio`; draft PR #18. Original base: `main` at `6baab35`.

## Current direction

Six freestanding physical media objects on a quiet gray surface. The interface uses indigo links and neutral text. The objects have their own colors: yellow floppy, blue cassette, green cartridge, peach record sleeve, lavender book, and ivory/teal floppy. A compact header contains the supplied avatar, name, About link, and theme toggle.

Articles and About are conventional full pages. The intercepted route slot and reader dialog have been removed. Each page has a collection link near the top and bottom; the name/avatar also returns home. Browser navigation uses normal history.

## Content

The existing Vibe RL article and its screenshots remain intact. Five new sourced draft explainers cover reward design and RL, evaluation, Transformer architecture, formal proof checking, and world models. They are labeled as draft notes and excluded from the sitemap until editorial review. The text separates established descriptions from illustrative examples and proposed evaluation practices. It does not invent research results or attribute new experiments to the site owner.

Each draft has an original SVG technical illustration, with alt text. The diagrams are generated by `scripts/draw-note-figures.py` using only Python's standard library. References are linked in each article. Figures are explanatory schematics, not empirical charts or reproductions of paper figures.

## Review decisions

- Kept the sparse collection UX and small title captions.
- Restored distinct object colors; blue is the UI accent, not a restriction on artwork.
- Removed all article/About overlays per feedback.
- Gave articles a 700px maximum reading width and relevant figures.
- Reviewed rendered diagrams for clipping, label spacing, and the relationships they depict.
- Kept three columns on desktop and two on phones, with scaling at 800, 600, and 370px.

## Development

```sh
npx pnpm@8.15.9 install --frozen-lockfile
python scripts/draw-note-figures.py
npm run lint
npm run build
```

No dependencies added. Production main remains unchanged. The user reviews the protected Vercel preview; do not request browser sign-in.
